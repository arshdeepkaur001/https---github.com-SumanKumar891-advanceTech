import 'package:flutter/material.dart';

class ConfigureRana extends StatelessWidget {
  final String deviceId;

  const ConfigureRana({super.key, required this.deviceId});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Rana'),
    );
  }
}
